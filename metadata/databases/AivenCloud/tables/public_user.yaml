table:
  name: user
  schema: public
object_relationships:
  - name: subscription
    using:
      foreign_key_constraint_on:
        column: userid
        table:
          name: subscription
          schema: public
